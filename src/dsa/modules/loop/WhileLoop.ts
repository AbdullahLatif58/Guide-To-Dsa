// this is soecially used for the unknown numbers iteration man 
// this is used for when u dont know how many times u loop
// Reading a file in chunks.
// Receiving messages until the socket closes.
// Polling an API until a job completes.
function getPassword(): void {
   
}
let password: any = ""

while (password !== "123") {
    password = getPassword();
}

